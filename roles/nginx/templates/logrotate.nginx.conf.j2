{{ brew_install_path }}/var/log/nginx/*.log {{ brew_install_path }}/var/log/nginx/*.err {
  size 1M
  missingok
  rotate 7
  daily
  compress
  delaycompress
  copytruncate
  notifempty
  postrotate
    {{ brew_install_path }}/bin/nginx -s reload > /dev/null
  endscript
}
