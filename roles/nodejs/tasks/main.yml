---
- name: "Remove old node packages"
  homebrew: 
    name: "{{ item }}"
    state: absent
  tags:
    - always
  with_items:
    - nodejs
    - node
    - npm
    
- name: "Installing nodejs"
  homebrew: 
    name: node 
    state: present
    install_options: "without-icu4c"
  tags:
    - always
