sudo: true
os:
  - osx
branches:
  only:
    - master
matrix:
  include:
    - env: TAGS=php56,zsh
    - env: TAGS=php70,zsh
    - env: TAGS=php71,zsh
    - env: TAGS=nodejs
script:
  - travis_wait 60 $TRAVIS_BUILD_DIR/run.sh -v --tags "${TAGS}"
